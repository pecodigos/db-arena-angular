import { ScaleContentDirective } from './scale-content.directive';

describe('ScaleContentDirective', () => {
  it('should create an instance', () => {
    const directive = new ScaleContentDirective();
    expect(directive).toBeTruthy();
  });
});
