import { PreventDragDirective } from './prevent-drag.directive';

describe('PreventDragDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventDragDirective();
    expect(directive).toBeTruthy();
  });
});
